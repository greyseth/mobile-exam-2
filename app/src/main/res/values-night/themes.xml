<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EcommerceApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="android:textColorHint">@color/black_half</item>"
        <item name="android:statusBarColor">@color/primary_col</item>
        <item name="statusBarBackground">@color/primary_col</item>
    </style>
</resources>